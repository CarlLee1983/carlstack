---
title: "現代 Lakehouse 核心架構對決：Apache Iceberg vs. Delta Lake vs. Apache Hudi 底層技術深析"
description: "深入現代資料湖倉一體（Lakehouse）底層核心：為什麼直接在 S3/GCS 讀寫 Parquet 會發生數據不一致？開放表格格式（Open Table Formats）三國大對決：Apache Iceberg（元數據樹與隱藏分區）、Delta Lake（ACID 事務日誌）與 Apache Hudi（流批一體 Copy-on-Write / Merge-on-Read）深度對比與選型指南。"
publishDate: 2026-09-02T16:25:00+08:00
draft: false
featured: false
cover: ../../assets/covers/lakehouse-architecture-iceberg-vs-delta-vs-hudi.png
coverAlt: "現代 Lakehouse 核心架構 Iceberg vs Delta vs Hudi"
tags:
  - "大數據"
  - "Lakehouse"
  - "Apache Iceberg"
  - "Delta Lake"
  - "Apache Hudi"
  - "系統設計"
repositoryUrl: https://github.com/ByteByteGoHq/system-design-101
---

在早期以 Hadoop / Hive 為核心的資料湖時代，資料以 Parquet 或 ORC 列式檔案直接存儲於 HDFS 或 AWS S3 物件儲存中。

然而，單純的物件儲存存在三大硬傷：

1. **缺少 ACID 事務保證**：多個任務同時寫入或更新時，讀取端極易讀取到寫到一半的「髒數據」；
2. **目錄級元數據瓶頸**：Hive 依賴目錄路徑進行分區查找，當資料量破千萬個檔案時，僅 `S3 LIST` 目錄就需耗費數十分鐘；
3. **無法時間旅行（Time Travel）與更新刪除（UPSERT / DELETE）**：對於 GDPR 被遺忘權或資料修正極度痛苦。

為此，業界誕生了 **現代開放表格格式（Open Table Formats）**，正式開啟了 **湖倉一體（Lakehouse）** 時代。

當前開源世界形成了「三國鼎立」格局：**Apache Iceberg**、**Delta Lake** 與 **Apache Hudi**。本文將為你深入剖析這三大表格格式的底層元數據結構與架構權衡。

---

## 1. 三大 Table Formats 核心架構矩陣

| 核心特性           | Apache Iceberg (Netflix / Apple 主推)                                 | Delta Lake (Databricks 主推)                                | Apache Hudi (Uber 主推)                        |
| :----------------- | :-------------------------------------------------------------------- | :---------------------------------------------------------- | :--------------------------------------------- |
| **元數據組織方式** | **樹狀快照結構 (Metadata Tree: Catalog ➔ Manifest List ➔ Manifests)** | 單一交易日誌目錄 (`_delta_log/*.json` + Checkpoint Parquet) | 時間線 (Timeline) 與 Commit 狀態檔案           |
| **事務隔離級別**   | 快照隔離（Snapshot Isolation / OCC）                                  | 串行化 / 寫入快照隔離                                       | 快照隔離 / 樂觀鎖                              |
| **寫入更新模式**   | Copy-on-Write (Positional/Equality Delete)                            | Copy-on-Write 與 Deletion Vectors                           | **Copy-on-Write (COW) 與 Merge-on-Read (MOR)** |
| **分區機制**       | **隱藏分區（Hidden Partitioning，無須在 SQL 顯式宣告）**              | 物理目錄分區 (`date=2026-09-02/`)                           | 物理目錄分區                                   |
| **主要優勢場景**   | **跨引擎通用性極佳（Trino, Spark, Flink, StarRocks）**                | 與 Spark / Databricks 生態深度整合                          | **高頻即時 Upsert / CDC 增量入湖**             |

---

## 2. Apache Iceberg 底層結構：樹狀元數據體系

Iceberg 徹底拋棄了「目錄即分區」的傳統 Hive 思維，將表格狀態抽象為一個精密的樹狀層級：

<div class="my-8 overflow-hidden rounded-xl border border-border bg-card p-4 sm:p-6">
  <svg viewBox="0 0 800 290" class="w-full h-auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="iceberg-meta-title iceberg-meta-desc">
    <title id="iceberg-meta-title">Apache Iceberg 樹狀元數據體系（Metadata Tree）架構圖</title>
    <desc id="iceberg-meta-desc">展示 Catalog 指針指向 Table Metadata，經 Manifest List、Manifest Files 最終定位底層 Parquet 資料檔。</desc>
    
    <defs>
      <marker id="lake-arrow" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 1 L 8 5 L 0 9 z" fill="#58a6ff"/>
      </marker>
    </defs>

    <rect x="220" y="10" width="360" height="38" rx="8" fill="#1f242c" stroke="#58a6ff" stroke-width="1.5"/>
    <text x="400" y="34" fill="#58a6ff" font-size="13" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">Iceberg Catalog (AWS Glue / REST / Nessie)</text>

    <path d="M 400 48 L 400 70" stroke="#58a6ff" stroke-width="2" marker-end="url(#lake-arrow)"/>

    <rect x="180" y="70" width="440" height="42" rx="8" fill="#161b22" stroke="#bc8cff" stroke-width="1.5"/>
    <text x="400" y="96" fill="#d2a8ff" font-size="13" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">Table Metadata (v3.metadata.json - Schema, 分區規格, 快照清單)</text>

    <path d="M 400 112 L 400 135" stroke="#bc8cff" stroke-width="2" marker-end="url(#lake-arrow)"/>

    <rect x="200" y="135" width="400" height="42" rx="8" fill="#241b35" stroke="#bc8cff" stroke-width="1.2"/>
    <text x="400" y="161" fill="#d2a8ff" font-size="12" font-family="system-ui, sans-serif" font-weight="600" text-anchor="middle">Manifest List File (snap-101.avro - 記錄多個 Manifest 檔案)</text>

    <path d="M 330 177 L 230 205" stroke="#3fb950" stroke-width="1.5" marker-end="url(#lake-arrow)"/>
    <path d="M 470 177 L 570 205" stroke="#3fb950" stroke-width="1.5" marker-end="url(#lake-arrow)"/>

    <rect x="60" y="205" width="320" height="38" rx="6" fill="#1b2e23" stroke="#238636" stroke-width="1"/>
    <text x="220" y="229" fill="#3fb950" font-size="11" font-family="system-ui, sans-serif" text-anchor="middle">Manifest File 1 (記錄 Min/Max 統計)</text>

    <rect x="420" y="205" width="320" height="38" rx="6" fill="#1b2e23" stroke="#238636" stroke-width="1"/>
    <text x="580" y="229" fill="#3fb950" font-size="11" font-family="system-ui, sans-serif" text-anchor="middle">Manifest File 2 (記錄 Min/Max 統計)</text>

    <path d="M 220 243 L 220 260" stroke="#3fb950" stroke-width="1.5" marker-end="url(#lake-arrow)"/>
    <path d="M 580 243 L 580 260" stroke="#3fb950" stroke-width="1.5" marker-end="url(#lake-arrow)"/>

    <rect x="120" y="260" width="200" height="26" rx="4" fill="#161b22" stroke="#30363d"/>
    <text x="220" y="278" fill="#8b949e" font-size="10" font-family="monospace" text-anchor="middle">data1.parquet (資料列)</text>

    <rect x="480" y="260" width="200" height="26" rx="4" fill="#161b22" stroke="#30363d"/>
    <text x="580" y="278" fill="#8b949e" font-size="10" font-family="monospace" text-anchor="middle">data2.parquet (資料列)</text>

  </svg>
</div>

### 為什麼 Iceberg 查詢速度極快？

- **精確剪枝（Pruning）**：查詢引擎無需掃描任何 S3 目錄，只需在記憶體中讀取 Manifest 的 `Min/Max` 統計資訊，即可直接過濾掉 99% 無關的 Parquet 檔案！
- **隱藏分區（Hidden Partitioning）**：工程師可以用 `timestamp` 分區，用戶查詢 `WHERE event_time >= '2026-09-01'` 時，Iceberg 自動在底層轉換為天級分區剪枝，防止因忘記指定分區欄位而引發全表全量掃描。

---

## 3. Delta Lake 底層結構：交易日誌與 Checkpoint

Delta Lake 以 `_delta_log` 目錄下的 JSON 事務日誌為核心：

```text
_delta_log/
├── 00000000000000000000.json  (Commit 0: 建立表格，寫入 2 個 Parquet)
├── 00000000000000000001.json  (Commit 1: 刪除 1 個 Parquet，新增 1 個 Parquet)
├── ...
└── 00000000000000000010.checkpoint.parquet (每 10 次 Commit 壓縮為一個 Checkpoint)
```

- **時間旅行（Time Travel）**：只需重放從 0 到指定 Commit 的交易日誌，即可精確重現歷史任意時間點的表格狀態（`SELECT * FROM table VERSION AS OF 5`）。

---

## 4. Apache Hudi 特化模式：Merge-on-Read (MOR)

Hudi 專為高頻即時寫入（CDC 入湖）打造了 **MOR（讀時合併）** 模式：

<div class="my-8 overflow-hidden rounded-xl border border-border bg-card p-4 sm:p-6">
  <svg viewBox="0 0 800 240" class="w-full h-auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="hudi-mor-title hudi-mor-desc">
    <title id="hudi-mor-title">Apache Hudi Merge-on-Read (MOR) 即時讀寫架構圖</title>
    <desc id="hudi-mor-desc">展示即時寫入追加 Avro Delta 日誌，查詢端可選擇只讀 Parquet 或動態合併 Parquet+Avro，非同步 Worker 執行壓實。</desc>
    
    <defs>
      <marker id="hudi-arrow" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 1 L 8 5 L 0 9 z" fill="#58a6ff"/>
      </marker>
    </defs>

    <rect x="20" y="15" width="220" height="42" rx="8" fill="#1f242c" stroke="#58a6ff" stroke-width="1.5"/>
    <text x="130" y="41" fill="#58a6ff" font-size="13" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">即時寫入流 (Flink / Spark)</text>

    <path d="M 240 36 L 310 36" stroke="#58a6ff" stroke-width="2" marker-end="url(#hudi-arrow)"/>

    <rect x="320" y="15" width="460" height="85" rx="10" fill="#161b22" stroke="#d29922" stroke-width="1.5"/>
    <text x="550" y="40" fill="#e3b341" font-size="13" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">基礎列式檔案 + 增量行式日誌 (MOR 儲存層)</text>
    <text x="340" y="65" fill="#3fb950" font-size="11" font-family="monospace">• base_data.parquet (歷史歷史快照)</text>
    <text x="340" y="85" fill="#d2a8ff" font-size="11" font-family="monospace">• delta_log.avro (即時 Append 追加日誌，極速入湖！)</text>

    <path d="M 400 100 L 220 135" stroke="#58a6ff" stroke-width="1.5" marker-end="url(#hudi-arrow)"/>
    <path d="M 550 100 L 550 135" stroke="#3fb950" stroke-width="1.5" marker-end="url(#hudi-arrow)"/>
    <path d="M 700 100 L 700 135" stroke="#bc8cff" stroke-width="1.5" marker-end="url(#hudi-arrow)"/>

    <rect x="30" y="135" width="230" height="85" rx="8" fill="#161b22" stroke="#388bfd" stroke-width="1.2"/>
    <text x="145" y="160" fill="#58a6ff" font-size="12" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">Read Optimized 查詢</text>
    <text x="145" y="180" fill="#c9d1d9" font-size="11" font-family="system-ui, sans-serif" text-anchor="middle">只讀 Parquet 檔案</text>
    <text x="145" y="200" fill="#8b949e" font-size="10" font-family="system-ui, sans-serif" text-anchor="middle">毫秒級響應・微小延遲</text>

    <rect x="280" y="135" width="240" height="85" rx="8" fill="#1b2e23" stroke="#238636" stroke-width="1.5"/>
    <text x="400" y="160" fill="#3fb950" font-size="12" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">Snapshot Query (即時)</text>
    <text x="400" y="180" fill="#3fb950" font-size="11" font-family="system-ui, sans-serif" text-anchor="middle">動態合併 Parquet + Avro</text>
    <text x="400" y="200" fill="#56d364" font-size="10" font-family="system-ui, sans-serif" text-anchor="middle">100% 嚴格最新資料</text>

    <rect x="540" y="135" width="240" height="85" rx="8" fill="#241b35" stroke="#bc8cff" stroke-width="1.2"/>
    <text x="660" y="160" fill="#d2a8ff" font-size="12" font-family="system-ui, sans-serif" font-weight="700" text-anchor="middle">Compaction Worker</text>
    <text x="660" y="180" fill="#c9d1d9" font-size="11" font-family="system-ui, sans-serif" text-anchor="middle">非同步背景壓縮壓實</text>
    <text x="660" y="200" fill="#8b949e" font-size="10" font-family="system-ui, sans-serif" text-anchor="middle">合併產出全新 Parquet</text>

  </svg>
</div>

---

## 5. 架構選型指南

- **選 Apache Iceberg**：如果你的企業是多引擎架構（既用 Spark 跑 ETL，又用 Trino / Presto 做互動分析，還用 Flink / StarRocks 查即時報表），Iceberg 的生態相容性與元數據設計是目前業界公認的最佳選擇。
- **選 Delta Lake**：如果團隊深度依賴 Databricks 生態與 Apache Spark，Delta Lake 的開箱即用體驗與優化最為絲滑。
- **選 Apache Hudi**：如果業務有著極端頻繁的 MySQL / PostgreSQL CDC 逐行 Upsert / Delete 增量同步需求。
